# Excel Workbook

Run `python python/vendor_eda.py` to generate `vendor_performance_model.xlsx`.

The workbook contains:

- Vendor Scorecard with engineered KPIs and recommendations
- Category Summary for pivot tables
- Region Summary for stakeholder review

This folder is intentionally committed with instructions instead of a generated binary workbook so the repository stays lightweight and reviewable.
